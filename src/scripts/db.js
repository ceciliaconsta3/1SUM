// a Javascript module with a specific responsibility

var sumDB = (function() { // anonymous function creates new module sumDB
    var sDB = {}; // empty js object used to store all of the methods in teh module to be acessible from outside the scope of the module
    var datastore = null;

    //TODO: add methods for interacting with the database here

    // export the tDB object
    return sDB;
}());